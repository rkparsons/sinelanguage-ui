enum Import {
    DOCUMENT = `import { Document } from '@contentful/rich-text-types/dist/types/types'`,
    FLUID_OBJECT = `import { FluidObject } from 'gatsby-image'`,
    GRAPH_QL = `import { graphql } from 'gatsby'`,
}

export default Import
