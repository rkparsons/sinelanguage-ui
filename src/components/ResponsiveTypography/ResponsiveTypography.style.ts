import { Typography as MuiTypography } from '@material-ui/core'
import { responsiveStyles } from '~/styles/css'
import styled from 'styled-components'

export const Typography = styled(MuiTypography)`
    ${responsiveStyles}
`
