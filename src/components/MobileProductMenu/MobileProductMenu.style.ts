import { Paper } from '@material-ui/core'
import styled from 'styled-components'

export const Popup = styled.div`
    all: revert;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 3000;
`
